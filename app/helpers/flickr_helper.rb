module FlickrHelper

end


# get_user_id(user_name) # use an inputted username to get the user's ID #

# get_photos(user_id, limit) # use a user's ID # to get X number of photos from their feed

# show_photos # show the photos from the user's stream